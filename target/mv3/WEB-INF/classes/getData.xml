<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="java/pojo">
    <select id="getSiteFromDB" resultType="Site">
        select longitude,latitude from jizhan
    </select>
    <select id="getManFromDB" resultType="man">
        select * from dem
    </select>

</mapper>